# 0.1.4 验证记录

- `npm run check`：通过。
- `npm test`：22 项通过。覆盖字幕片段合句、附近三句、A1 与未来一句展示、暂停优先请求、播放/暂停独立缓存、恢复时忽略迟到结果、暂停跳转不重复提交、错误后退出暂停模式。
- 浏览器使用真实 `panel.js` / `panel.css` 和本地模拟数据：测量宽度 280px；标题栏拖动有效；CSS 加载完成后恢复保存的位置；追加 12 张卡片后自动滚到底部；暂停时禁用等级选择并临时隐藏“已掌握”；恢复播放后恢复；同一张卡片可补充词内本义。
- 真实 ChatGPT 订阅、gpt-5.6-luna：播放模式返回 3 个表达，首条约 6.4 秒；暂停模式返回 15 个表达（含 A1），首条约 6.8 秒。`take clean water for granted` 含 `granted` 的“被给予的；被承认的”本义。

本次没有在用户正在看的 YouTube 页面重新加载扩展后做完整端到端测试。更新生效需重新加载 Luna，再刷新 YouTube；原生程序仍使用同一本地目录，无须重新安装。暂停时已有结果立即显示，未分析的词仍受模型延迟影响。只有滚动字幕时，无法提前取得尚未出现的句子；无标点字幕按停顿或短时间段组成句组。
